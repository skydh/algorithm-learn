1.新增数据导致子节点的数据大于某个阈值时,采用分裂的思想来处理。就是不断分裂，使其满足要求。
2.删除数据导致子节点数据小于一个阈值时，那就采用合并的策略。




以下是b+树的特性:
1.每个节点的数目不能超过m,也不能小于m/2。
2.根节点的子节点个数可以不超过m/2。
3.m叉树只存储索引，并不存储真正的数据。
4.通过链表将子节点连在一起，方便范围查询。
5.根节点存在内存，其他节点存在银盘